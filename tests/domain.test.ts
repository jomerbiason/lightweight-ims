import {describe,it,expect} from "vitest";import {status,estimatedProfit,unreversedSales} from "../src/domain";
const p:any={id:"p",name:"Coke",sellingPrice:85,costPrice:70,stock:4,reorderLevel:5,targetStock:20};
describe("inventory rules",()=>{it("detects status",()=>{expect(status({...p,stock:0})).toBe("out");expect(status({...p,stock:5})).toBe("low");expect(status({...p,stock:6})).toBe("normal")});it("calculates estimated profit",()=>{const x=estimatedProfit([{id:"t",productId:"p",type:"SALE",quantityChange:-2,previousQuantity:4,newQuantity:2,timestamp:""}] as any,[p]);expect(x.salesTotal).toBe(170);expect(x.cost).toBe(140);expect(x.profit).toBe(30)});
it("excludes reversed sales from unreversedSales",()=>{const tx:any=[{id:"t1",productId:"p",type:"SALE",quantityChange:-2,previousQuantity:4,newQuantity:2,timestamp:"1"},{id:"t2",productId:"p",type:"REVERSAL",quantityChange:2,previousQuantity:2,newQuantity:4,timestamp:"2",referenceId:"t1"},{id:"t3",productId:"p",type:"SALE",quantityChange:-1,previousQuantity:4,newQuantity:3,timestamp:"3"}];const result=unreversedSales(tx);expect(result.map((t:any)=>t.id)).toEqual(["t3"])});
it("does not count a reversed sale toward estimated profit",()=>{const tx:any=[{id:"t1",productId:"p",type:"SALE",quantityChange:-2,previousQuantity:4,newQuantity:2,timestamp:"1",salePrice:85,saleCost:70},{id:"t2",productId:"p",type:"REVERSAL",quantityChange:2,previousQuantity:2,newQuantity:4,timestamp:"2",referenceId:"t1"}];const x=estimatedProfit(unreversedSales(tx),[p]);expect(x.salesTotal).toBe(0);expect(x.profit).toBe(0)})})