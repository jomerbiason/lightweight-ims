{
  "name": "lightweight-ims",
  "version": "1.3.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "verify": "node scripts/verify.mjs",
    "worker:dev": "wrangler dev --local",
    "deploy": "vite build && wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.3.6",
    "vitest": "^3.2.7",
    "wrangler": "^4.125.0"
  }
}
